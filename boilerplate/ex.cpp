#include<stdio.h>
#include<string>
#include <iostream>

using namespace std; 

int main(){
    string msg, crib;
    cin >> msg >> crib;

    int limite = msg.size() - crib.size(), total = 0;
    cout << total << endl;
    return 0;
}